engines:
  rubocop:
    enabled: true
    channel: rubocop-0-67
    config:
      file: .rubocop.yml
    exclude_paths:
      - 'vendor/'
      - 'db/'

exclude_paths:
  - 'public/assets/'
  - 'db/'

ratings:
  paths:
    - 'app/**/*'
    - 'lib/**/*'
    - '**.rb'
